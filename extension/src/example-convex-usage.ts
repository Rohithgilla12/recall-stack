// Example of how to use the shared Convex API in the extension
import { api } from "../convex/_generated/api"

// You can now use the api object to call your Convex functions
// For example:
// const products = await convexClient.query(api.products.get);

export { api }
